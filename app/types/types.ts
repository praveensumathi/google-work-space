export type PricePlans = {
  heading: string;
  options: string[];
};
export type ApplicationList = {
  imageUrl: string;
  title: string;
  description: string;
};
export type ServiceList = {
  imageUrl: string;
  title: string;
};
